# Copy to .env and fill in. .env is gitignored.
# Load into a shell with: set -a; . ./.env; set +a

# --- marimo ---------------------------------------------------------------
# Token for authenticated marimo servers (the marimo-pair skill reads this).
# MARIMO_TOKEN=

# --- R / rpy2 -------------------------------------------------------------
# Use the ABI interface when the rpy2 wheel was built against a different R.
# RPY2_CFFI_MODE=ABI
# R_HOME=$(R RHOME)

# --- data sources ---------------------------------------------------------
# DATABASE_URL=postgresql://user:pass@localhost:5432/db
# S3_BUCKET=my-bucket
